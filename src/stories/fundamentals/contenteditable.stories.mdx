import { InvestigateInputEvent, BasicContentEditableDiv } from './contenteditable'

<Meta title="Fundamentals/Contenteditable" />

# contenteditable

By adding `contenteditable` to an HTML element it can be made editable:

<!-- TODO: Add HTML source code next to element -->

<BasicContentEditableDiv>I am an editable div</BasicContentEditableDiv>

# onInput

Whenever there is a change on a `contenteditable` element an `InputEvent` is fired. You can listen on them via `onInput`:

<InvestigateInputEvent/>
